# Core - Standard library only, no runtime dependencies needed
# All functionality uses Python standard library

# Build
pyinstaller>=6.0.0

# Development (optional, install via: pip install -r requirements-dev.txt)
# pytest>=8.0.0
# flake8>=7.0.0